import styles from './SettingsPage.module.css';

/** Пустая страница настроек (заглушка до реализации). */
export const SettingsPage = () => {
  return (
    <div className={styles.page}>
      <h1 className={styles.placeholder}>НАСТРОЙКИ</h1>
    </div>
  );
};
