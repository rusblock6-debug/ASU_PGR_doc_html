/**
 * Визуальные параметры 3D-сцены карты.
 */
export const MAP_SCENE = {
  /** Минимальная дистанция зума камеры. */
  CAMERA_MIN_DISTANCE: 100,

  /** Максимальная дистанция зума камеры. */
  CAMERA_MAX_DISTANCE: 1800,

  /** Длительность анимации перелёта камеры (секунды). */
  CAMERA_FOCUS_DURATION: 0.8,

  /** Множитель приближения камеры за один шаг (для отдаления — `1 / ZOOM_STEP`). */
  ZOOM_STEP: 0.8,

  /** Y-координата слоя подложки. */
  BACKGROUND_Y: 0,

  /** Y-координата слоя дорожного графа. */
  ROAD_GRAPH_Y: 1,

  /** Y-координата слоя мест. */
  PLACES_Y: 2,

  /** Y-координата слоя транспорта. */
  VEHICLES_Y: 3,

  /** Y-координата слоя Линейки. */
  RULER_Y: 4,

  /** Смещение точек по Y-координате над плоскостью слоя. */
  POINT_ABOVE_LAYER_Y: 0.02,

  /** Дистанция камеры, ближе которой HTML-элементы начинают уменьшаться. */
  PROXIMITY_THRESHOLD: 200,

  /** Минимальный визуальный масштаб HTML-элементов при максимальном приближении камеры. */
  MIN_PROXIMITY_SCALE: 0.95,

  /** Базовый масштаб HTML-элементов на карте (уменьшает исходный CSS-размер). */
  BASE_HTML_SCALE: 0.85,

  /** Дефолтные визуальные параметры дорожного графа. */
  ROAD_GRAPH: {
    /** Цвет ребра. */
    EDGE_COLOR: '#6A848B',
    /** Ширина ребра. */
    EDGE_WIDTH: 7,
    /** Цвет вершины. */
    NODE_COLOR: '#6A848B',
    /** Размер вершины. */
    NODE_SIZE: 5,
    /** Цвет обводки вершины. */
    NODE_BORDER_COLOR: '#FEFCF9',
    /** Цвет обводки вершины при наведении. */
    NODE_HOVER_BORDER_COLOR: '#D15C29',
    /** Цвет ghost-точки при вставке на сегмент. */
    GHOST_NODE_COLOR: '#D15C29',
    /** Цвет обводки ghost-точки. */
    GHOST_NODE_BORDER_COLOR: '#FEFCF9',
    /** Размер границы ghost-точки. */
    GHOST_NODE_BORDER_WIDTH: 1,
  },

  /** Параметры камеры сцены. */
  CAMERA: {
    /** Начальная позиция `[x, y, z]`. */
    INITIAL_POSITION: [0, 1500, 0.01],
    /** Начальная точка, на которую смотрит камера `[x, y, z]`. */
    INITIAL_TARGET: [0, 0, 0],
    /** Вертикальное поле зрения (в градусах). */
    FOV: 50,
    /** Минимальная дистанция видимости камеры. */
    NEAR: 1,
    /** Максимальная дистанция видимости камеры. */
    FAR: 5000,
  },
} as const;

/** Базовый азимут для ориентации компаса (направление "Север" в стартовой позиции камеры). */
export const NORTH_AZIMUTH = Math.atan2(
  MAP_SCENE.CAMERA.INITIAL_POSITION[0] - MAP_SCENE.CAMERA.INITIAL_TARGET[0],
  MAP_SCENE.CAMERA.INITIAL_POSITION[2] - MAP_SCENE.CAMERA.INITIAL_TARGET[2],
);
