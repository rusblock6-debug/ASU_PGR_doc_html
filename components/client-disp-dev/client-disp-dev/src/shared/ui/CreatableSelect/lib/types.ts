import type { SelectOption } from '@/shared/ui/types';

/** Значение для опции «Создать». */
export const CREATE_OPTION_VALUE = '__create__';

/** Состояние редактирования опции. */
export type EditingState = SelectOption | null;
